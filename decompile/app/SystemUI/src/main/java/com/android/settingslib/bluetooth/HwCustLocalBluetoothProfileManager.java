package com.android.settingslib.bluetooth;

import android.bluetooth.BluetoothDevice;
import android.content.Context;
import android.content.Intent;

public class HwCustLocalBluetoothProfileManager {
    public HwCustLocalBluetoothProfileManager(Context context, CachedBluetoothDeviceManager deviceManager) {
    }

    public void handleCustIntent(Intent intent, BluetoothDevice device) {
    }
}
