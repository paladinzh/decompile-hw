package com.huawei.keyguard.events;

import android.content.Context;
import android.content.Intent;

public interface MessageCenter$IEventReceiver {
    boolean onReceive(Context context, Intent intent);
}
