package com.android.contacts.list;

public enum ContactTileAdapter$DisplayType {
    STREQUENT,
    STREQUENT_PHONE_ONLY,
    STARRED_ONLY,
    FREQUENT_ONLY,
    GROUP_MEMBERS,
    SMART_GROUP_ONLY
}
