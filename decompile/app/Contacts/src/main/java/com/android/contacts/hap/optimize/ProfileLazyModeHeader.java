package com.android.contacts.hap.optimize;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.LinearLayout;

public class ProfileLazyModeHeader extends LinearLayout {
    public ProfileLazyModeHeader(Context context) {
        super(context);
    }

    public ProfileLazyModeHeader(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public ProfileLazyModeHeader(Context context, AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
    }
}
